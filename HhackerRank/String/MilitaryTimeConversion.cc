#include<bits/stdc++.h>
using namespace std;

string timeConversion(string s) {
    string res= "10:30PM";
// __s – C string to locate.
// __pos – Index of character to search from.
// __n – Number of characters from s to search for.
    cout << s.find("pm",0,res.length);
    return res;
}   

int main (){

return 0;
}